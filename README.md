# 🌾 Crop Yield Prediction

A machine-learning driven analytics pipeline engineered to forecast crop yield using weather data, soil characteristics, agronomic practices, and satellite vegetation indices.

---

## 🚀 Key Capabilities
- End-to-end data ingestion → preprocessing → feature engineering → modeling → evaluation.
- Supports multi-source datasets (weather, soil, NDVI, farming logs).
- Optimized ML models (Random Forest, XGBoost, LightGBM).
- Exportable model artifacts and reproducible pipeline configuration.
- Optional FastAPI endpoint for real-time inference.

---

## 📁 Project Structure
